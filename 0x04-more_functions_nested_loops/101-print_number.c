#include "holberton.h"

/**
  * print_number - Prints an integer.
  * @n: integer
  *
  * Return: 0
  */
void print_number(int n)
{
	if (n < 0)
	{
		n *= -1;
		_putchar('-');
		return (n);
	}

	if ((n == 0) && (n % 10 == 0)
		return (n);
}
