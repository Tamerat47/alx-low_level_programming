#include "main.h"
/**
 * is_prime_number - prime number take 1 otherwise 0
 * @n: the number integer
 * Return: always 1 and 0
 */
int is_prime_number(int n)
{
	if (n % 2 == 0 || n == 1 || n == 0)
		return (o);
	if (n % 3 == 0)
		return (0);
	else
		return (1);
}
