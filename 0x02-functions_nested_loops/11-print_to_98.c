#include <stdio.h>
#include "main.h"
/**
 * print_to_98 - print all natural number
 *
 * @n: number of natural number
 */
void print_to_98(int n)
{
	if (n > 98)
	{
		if (n > 98)
			printf("%d, ", n--);
	}
	else if (n == 98)
		printf("%d\n", n);
	else if
	{
		while (n < 98)
			printf("%d, ", n--);
	}
	else
	{
		printf("%d\n", n);
	}
}

