#include<stdio.h>
/**
 * main - print _putchar followed by a new line
 *
 * Return: always 0
 */
int main(void)
{
	int x = 0;
	char _putchar;

	for (x == 0; x++)
		while (x == 0)
		{
			putchar(x);
			putchar('\n');
		}
	return (0);
}
