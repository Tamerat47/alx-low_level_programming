#include "main.h"
/*
 * add - adds two integer
 *
 * @a: is an integer
 * @b: is an integer
 */
int add(int a, int b);
{
	int a, b, c;
	{
		c = a * b;

		if (c <= 9)
			putchar(c)
		else
			putchar((prod / 10) + '0');

			putchar((prod % 10) + '0');
	}
}
