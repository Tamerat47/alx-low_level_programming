#include "main.h"
/*
 * add - adds two integer
 *
 * @a: is an integer
 * @b: is an integer
 */
int add(int a, int b);
{
	int a, b, c;
	{
		c = a * b;

		if (c <= 9)
			_putchar(c)
		else
			_putchar((prod / 10) + '0');

			_putchar((prod % 10) + '0');
	}
}
